[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    -m "not integration"

markers =
    integration: marks tests that call external APIs (PubChem, ChEMBL, UniProt, PDB) — run with -m integration
