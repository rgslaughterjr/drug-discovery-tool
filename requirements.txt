# Core dependencies
python-dotenv>=1.0.0
jupyter>=1.0.0
ipython>=8.0.0

# LLM Provider Support
# Install only the packages for providers you'll use

# Anthropic (Direct API) - recommended for UT Austin
anthropic>=0.29.0

# AWS Bedrock (if institutional AWS account available)
boto3>=1.28.0

# OpenAI (including Azure OpenAI via base_url)
openai>=1.3.0

# Cohere
cohere>=5.0.0

# Together.ai
# (uses openai package for compatibility)

# Ollama (local LLMs)
# (uses openai package for compatibility)

# Optional: for enhanced Jupyter notebooks
nbconvert>=7.0.0
matplotlib>=3.5.0
